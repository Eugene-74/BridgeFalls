name: $name
version: $version
main: $group.${name}Plugin
description: $description
author: Eugene-74
website: https://github.com/Eugene-74/${name}
api-version: "$apiVersion"
folia-supported: true
softdepend: [Towny, SiegeWar]
permissions:
  bridgefalls.admin:
    description: "Allows use of admin commands."
    default: op